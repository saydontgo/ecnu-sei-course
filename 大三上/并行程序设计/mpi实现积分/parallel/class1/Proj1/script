Project 1:

项目描述：
本次项目主要采用MPI实现基于odd_even策略的数据的排序。
具体步骤如下：
1：首先进程0随机产生15个随机整数，进程0打印结果。
2：进程0通过MPI_Scatter分配给5个进程，每个进程被分到3个整数。
3：5个进程间分别按照书上的例子进行odd_even排序。进程间通讯采用MPI_Sendrecv函数。这里需要注意的是局部排序时需要采用两种策略1）数组形式与2）派生类型来实现零时结果的交换。
4：进程0通过MPI_Gather获得排序后的结果，并打印。
5：采用Barrier与Wtime形式打印计算时间。

作业需要提交材料：
1：程序源码与README文件；需打包并提交到homeworkecnu@163.com，标题需采用格式“并行程序proj1-姓名-学号”
2：实验文档，需打印上课时提交。

作业提交时间：11月19日上课前


程序编译与运行如下：
1： mpic++（或者mpicc）-o sort1 sort1.cpp   %数组
    mpiexec -n 5 ./sort1

2： mpic++（或者mpicc）-o sort2 sort2.cpp   %派生类型
    mpiexec -n 5 ./sort2




